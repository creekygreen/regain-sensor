If in Raspberry Pi, open terminal and input the following commands
$ source regain/bin/activate     - activates the virtual environment (regain)
$ cd regain-sensor               - changes directory to folder (regain-sensor)
$ python3 app.py                 - opens (app.py) or just use the run button on thonny