Requirements before running the application.

1. Activate the venv(regain) from the terminal using the command below
source regain/bin/activate // activates the venv(regain)

2. To run the application, activate the following commands using the terminal
cd webdb // directory of the app
python3 app.py // runs the app